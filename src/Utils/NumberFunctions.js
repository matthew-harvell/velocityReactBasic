class NumberFunctions {
    IsPrime = (number) =>{
        /* 
            A prime number is defined as any number that is > 1 and which is only divisible by itself and 1.

            Create a function here that returns false if the number is not prime and true if the number is prime.
        */
    }
}

module.exports = NumberFunctions;